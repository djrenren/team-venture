<Page x:Class="MedicalImager.CoronalReconstruction"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="SliceLayout">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Image x:Name="Orig" Grid.Column="0" Grid.Row="0" Source="{Binding Path=Current[0]}" >
            <Image.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="_Window This Image" Click="MenuItem_Click">
                        
                    </MenuItem>
                </ContextMenu>
            </Image.ContextMenu>
        </Image>
        <Image x:Name="Slice" Source="{Binding Path=Reconstruction}" Grid.Column="0" Grid.Row="1" Width="{Binding ActualWidth, ElementName=Orig, Mode=OneWay}"/>
        <Image Grid.Column="0" Grid.Row="1" />
        <Button x:Name="ToggleButton" Grid.Column="1" Grid.Row="1" Content="Toggle Control" Click="ToggleButton_Click"/>
    </Grid>
</Page>
